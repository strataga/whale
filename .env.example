# Database
DATABASE_URL=./whale.db

# NextAuth
NEXTAUTH_SECRET=change-me-to-a-random-string
NEXTAUTH_URL=http://localhost:3002

# Encryption at rest (REQUIRED for production)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=

# AI Provider (configure one — or set via Settings page in the UI)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# Optional IP allowlist for bot traffic (comma-separated IPs or CIDR)
WHALE_IP_ALLOWLIST=
